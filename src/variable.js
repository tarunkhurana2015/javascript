// variables
// cannot be a reserved keyword
// should be meaningful
// cannot start with the number
// cannot contain hypen or space.
let name = "Tony";
console.log(name);

let firstName = "";
let lastName = "";
